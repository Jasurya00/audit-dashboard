{
  "name": "audit-dashboard",
  "version": "1.0.0",
  "description": "Audit Dashboard with proxy server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build-harmony-app": "mkdir -p build && cp public/index.html build/index.html && cd build && tar -cf ../app.tar . && cd .."
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
