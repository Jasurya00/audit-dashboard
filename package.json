{
  "name": "audit-dashboard",
  "version": "1.0.0",
  "description": "Audit Dashboard with proxy server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "prebuild-harmony-app": "rm -rf src && mkdir -p src && cp public/index.html src/index.html",
    "build-harmony-app": "build-harmony --templateName plain",
    "prepublishOnly": "npm run prebuild-harmony-app && npm run build-harmony-app",
    "publish-harmony-app": "harmony deploy"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
